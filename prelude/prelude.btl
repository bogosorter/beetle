List<a> = ListConstructor (a, List<a>) | ListNil {};

(>>)(left: [a], right: [a]): [a] =
    if left is []: return right;

    head, tail = left;
    return head :: (tail >> right);

String = [Character];

export;
