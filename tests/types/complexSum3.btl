Sum = Left integer | Middle {x: integer, y: integer} | Right (integer, integer);

a = Left 1;
b = Right (1, 2);

if a is Left:
    if b is Middle: return a + b.y;
    if b is Right:
        _, b = b;
        return a + b;
    return -2;
return -1;
