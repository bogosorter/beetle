MyList = Cons (Integer, List) | Nil {};

length(list: MyList): Integer =
    if list is Nil: return 0;
    _, tail = list;
    return 1 + length(tail);

list = Cons (1, Cons (2, Cons (3, Nil {})));
return length(list);
