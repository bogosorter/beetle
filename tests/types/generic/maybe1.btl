Maybe<a> = Just a | Nothing {};

safeDiv(a: Integer, b: Integer): Maybe<Integer> =
    if b == 0: return Nothing {};
    return Just (a / b);

result = safeDiv(3, 0);
if result is Nothing: return -1;
return result;
