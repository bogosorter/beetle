type Maybe<a> {
    Just(a),
    Nothing
}

safeDiv(a: Integer, b: Integer) -> Maybe<Integer> {
    if b == 0: return Nothing;
    return Just(a / b);
}

result = safeDiv(3, 0);
match result {
    Just(n) -> n,
    Nothing -> -1
}
