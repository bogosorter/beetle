List = Cons (integer, List) | Nil {};

length(list: List): integer =
    case list of {
        Cons p ->
            _, tail = p;
            return 1 + length(tail);
        _ -> return 0;
    };

list = Cons (1, Cons (2, Cons (3, Nil {})));
return length(list);
