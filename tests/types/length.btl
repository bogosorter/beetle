List = Cons (integer, List) | Nil {};

length(list: List): List =
    case list of {
        Nil {} -> return 0;
        Cons p ->
            _, tail = p;
            return 1 + length(tail);
    };

list = Cons (1, Cons (2, Cons (3, Nil {})));
return length(list);
