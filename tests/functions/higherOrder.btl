s(n: integer): integer = return n + 1;

accumulate(f: (integer) -> integer, n: integer): integer =
    if n == 0: return 0;

    current = f(n);
    recursive = accumulate(f, n - 1);
    return current + recursive;

> accumulate(3);
