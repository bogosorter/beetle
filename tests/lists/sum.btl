sum(list: [Integer]) -> Integer {
    match list {
        ListNil -> 0,
        ListConstructor(constructor) -> {
            head, tail = constructor;
            return head + sum(tail);
        }
    }
}

list = [1, 2, 3];
return sum(list);
