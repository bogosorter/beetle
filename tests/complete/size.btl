Tree = Leaf integer | Node {left: Tree, right: Tree};

size(tree: Tree): integer =
    case tree of {
        Node tree ->
           return size(tree.left) + size(tree.right);
        _ -> return 1;
    };

tree = Node {
    left: Leaf 1,
    right: Node {
        left: Node {
            left: Leaf 2,
            right: Leaf 3
        },
        right: Leaf 4,
    }
};

return size(tree);
