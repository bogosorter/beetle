isTriangle(a: Integer, b: Integer, c: Integer): Boolean =
    if a + b <= c: return false;
    if b + c <= a: return false;
    if a + c <= b: return false;
    return true;

return isTriangle(3, 4, 5);
