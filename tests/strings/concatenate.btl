concatenate(a: String, b: String) -> String {
    match a {
        ListNil -> b,
        ListConstructor(constructor) -> {
            head, tail = constructor;
            return head :: concatenate(tail, b);
        }
    }
}

return concatenate('hello, ', 'world');
